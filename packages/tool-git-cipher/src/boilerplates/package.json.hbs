{
  "name": "@guanghechen/fake-repo",
  "version": "0.0.0",
  "private": true,
  "files": [],
  "scripts": {
    "cipher": "ghc-git-cipher",
    "encrypt": "yarn cipher encrypt",
    "encrypt:files": "yarn cipher encrypt --plainRootDir {{{plainRootDir}}}-files --filesOnly",
    "decrypt": "yarn cipher decrypt",
    "verify": "yarn cipher verify",
    "sync:pull": "(cd {{{cryptRootDir}}} && git pull origin main)",
    "sync:push": "(cd {{{cryptRootDir}}} && git push origin main)"
  }
}
